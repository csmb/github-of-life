{
  "name": "github-of-life",
  "version": "1.0.0",
  "description": "Conway's Game of Life on the GitHub contribution graph",
  "type": "module",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "seed": "node seed.js"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
